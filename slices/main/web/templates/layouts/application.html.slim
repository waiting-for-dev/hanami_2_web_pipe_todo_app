doctype html
html
  head
    meta charset="utf-8"
    title
      - if content_for :title
        ==> content_for :title
        ' /
      ' TodoManager
    meta name="title" content="TodoManager"
    link rel="stylesheet" type="text/css" href=assets["main__public.css"]
    script src=assets["main__public.js"] defer=true

  body
    .flash == flash[:success]
    == yield
